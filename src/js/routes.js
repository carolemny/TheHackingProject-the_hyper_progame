// listing des templates à afficher

const routes = {
  "": PageList,
  pagelist: PageList,
  game: PageDetail,
};

export { routes };
import { PageList } from "./PageList";
import { PageDetail } from "./PageDetail";
